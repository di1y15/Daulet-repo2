pipeline {
    agent any

    stages {

        stage("build") {

            steps {
                echo "building an app.."
            }
        }

        stage("test") {
            steps {
                echo "testing an app.."
            }
        }

        stage("deploy") {
            steps {
                echo "deployign an app.."
            }
        }

        stage
    }
}

